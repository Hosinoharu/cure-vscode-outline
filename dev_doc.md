这里记录开发过程中一些重要的问题。

# 只展开一项 TreeItem

在浏览 outline 时，预期中该功能是：同层级只展示一项，在点击不同项时，能自动折叠其它已经展开的。

以下我的想法。

首先要记录已经展开的 `TreeItem`，有以下存储：

-   记录已经展开的*最顶层*的 TreeItem，记为 `All_Expand_Top`
-   因为每一个层级最多只能展开一项，所以构建一个 Map 记为 `Expand_Map`，它的 `key` 是父 item 的 id，`value` 是展开的子项。如果展开一个 item 时，就这样做：
    -   先根据它的 parentId 获取对应父 item 下有没有展开的项目
    -   有的话就折叠它，然后展开自身、增加 map 记录



有了上面两个存储机制之后，当要展开另一个 `TreeItem A` 时：

1.   先向上不断展开 A 的父节点，直到找到 A 的顶层节点 `A_TOP` —— 整个过程不刷新 ui
2.   除了 `A_TOP` 之外，折叠其它所有的顶层节点（它们记录在 `All_Expand_Top` 中） —— 这时应该刷新对应节点的 ui
3.   折叠 A 的同层级节点（它们记录在 `Expand_Map` 中）然后展开 A —— 整个过程不刷新 ui
4.   在第 1 步时，会记录出最后一个发生了变化的、A 的父节点，**只需要刷新该父节点，就能实现 A 的展开以及与 A 同层级节点的折叠** —— 尽可能最小化刷新啦

# 语法符号 TreeView 的更新

在编辑文档时需要更新语法符号的 outline（它使用 `vscode TreeView` 展示），简单的处理当然是**重新获取当前文档的所有符号，然后刷新整个 TreeView**。

这种*最大化更新*的方案的问题自然是消耗更大，有时仅仅变化一个变量名就刷新整个 View，*这非常不适合用于更新现有文档的符号*，只有在切换文档等重大变化时，才需要重新刷新整个 View。

下面分情况来阐述我的想法。

## 顶层符号的变化

顶层符号（或者说顶层 TreeItem）指的是在 TreeView 中不存在父节点的符号，通常来说是全局符号。

### 个数发生变化

**如果顶层符号的个数发生了变化，没有办法，只能刷新整个 View 了**，因为 vscode TreeView 并没有什么添加、删除节点的 API，只能修改底层的数据然后刷新 View。

### 个数不变化

把现有的 `TreeItem` 当作是一个*容器* —— 它内部包含了一个符号 `symbol`，它只在以下情况下会刷新：

-   symbol 的名称变化
-   symbol 的种类（如变量、函数等）变化
-   symbol 是否具备子项目，也就是 TreeItem 左侧表示展开的箭头的变化

其它情况只需要更新 symbol 的位置（便于跳转）、重置 tooltip 为空（符号的注释）就行。

所以现在可以**根据符号的位置来更新每一个对应的顶层 TreeItem**，比如第一个 `symbol` 对应第一个 `TreeItem`。

注意，不能移动顶层节点，否则需要刷新整个 View，总之**只能改变顶层节点的属性而不能移动位置。**

那么，就可以这样做：

-   逐一按位置比较新旧的顶层符号 symbol
-   如果它们的 `name、kind` 变化，就说明该 item 需要进行重新刷新，就更新可能变化的信息
-   然后重新去填充 TreeItem 中的 symbol

```js
// 可以使用这样的数据结构来记录
[
    {
        new: symbol, // 记录新的符号
        // 记录是否发生了需要刷新 ui 的变化，后续将读取 symbol 属性后再刷新 item
        refresh: boolean
    },
    // 其它的符号
]
```



## 子项符号的变化

*只有在顶层符号的个数不变化时*，才考虑子项符号的处理，并且子 item 可以增加、删除、移动等等操作，最后只需要刷新父 item 就可以了。

我只考虑同层级的子项，不会到处查找、然后移动子项。因为 vscode 提供的符号并没有唯一的 ID，我也很难生成一个唯一的 ID，所以对同层级的子项，**就相当于是特殊的顶层符号，但区别是，如果父层级要刷新，那么子项就不要刷新了**。
